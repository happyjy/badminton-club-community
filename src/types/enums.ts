export enum Role {
  ADMIN = 'ADMIN',
  MEMBER = 'MEMBER',
}

export enum Status {
  PENDING = 'PENDING',
  APPROVED = 'APPROVED',
  ON_LEAVE = 'ON_LEAVE', // 휴가
  //
  REJECTED = 'REJECTED',
  LEFT = 'LEFT', // 탈퇴
}
